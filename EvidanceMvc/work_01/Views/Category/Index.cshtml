@*@model IEnumerable<work_01.Models.Category>
@{
    ViewBag.Title = "Index";
}
<div class="row">
    <div class="col">
        <h1>Category List</h1>
        <div class="d-flex justify-content-end mb-2">
            <a href="@Url.Action("Create","Category")" class="btn btn-info">Add New</a>
        </div>
        <div class="col-md-12">
            <table class="table table-bordered table-striped table-hover">
                <thead class="table-light">
                    <tr>
                        <th>@Html.DisplayNameFor(x => x.categoryId)</th>
                        <th>@Html.DisplayNameFor(x => x.categoryName)</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var c in Model)
                    {
                    <tr>
                        <td>@Html.DisplayFor(x => c.categoryId)</td>
                        <td>@Html.DisplayFor(x => c.categoryName)</td>
                        <td>
                            <a href="@Url.Action("Edit","Category",new { id=c.categoryId})" class="btn btn-info btn-sm">Edit</a>
                            <a href="@Url.Action("Delete","Category",new { id=c.categoryId})" class="btn btn-danger btn-sm">Delete</a>
                        </td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>*@

